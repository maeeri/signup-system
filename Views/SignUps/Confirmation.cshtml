
@model SignUpProject.Models.ViewModel
@inject IViewLocalizer Localizer
@inject IOptions<RequestLocalizationOptions> LocOptions

@{
    ViewData["Title"] = Localizer["title"];
}
<div class="contents">
    <h1>@Localizer["thanks"]</h1>
    <h3>@Localizer["camper"]</h3>
    
    <div>
        <h5>@Localizer["camps"]</h5>
        @{
            foreach (var camp in Model.Camps)
            {
                <p>@camp.Name @camp.Start.ToString("d.M.yyyy")-@camp.End.ToString("d.M.yyyy")</p>
            }
        }
        
        <hr/>

        <h5>@Localizer["contacts"]</h5>

        <dl class="row">
            <dt class="col-sm-2">
                @Localizer["firstname"]
            </dt>
            <dd class="col-sm-10">
                @Html.DisplayFor(model => model.Camper.FirstName)
            </dd>
            <dt class="col-sm-2">
                @Localizer["lastname"]
            </dt>
            <dd class="col-sm-10">
                @Html.DisplayFor(model => model.Camper.LastName)
            </dd>
            <dt class="col-sm-2">
                @Localizer["street"]
            </dt>
            <dd class="col-sm-10">
                @Html.DisplayFor(model => model.Camper.StreetAddress)
            </dd>
            <dt class="col-sm-2">
                @Localizer["postcode"]
            </dt>
            <dd class="col-sm-10">
                @Html.DisplayFor(model => model.Camper.PostalCode)
            </dd>
            <dt class="col-sm-2">
                @Localizer["city"]
            </dt>
            <dd class="col-sm-10">
                @Html.DisplayFor(model => model.Camper.City)
            </dd>
            <dt class="col-sm-2">
                @Localizer["guardianname"]
            </dt>
            <dd class="col-sm-10">
                @Html.DisplayFor(model => model.Guardian.Name)
            </dd>
            <dt class="col-sm-2">
                @Localizer["tel"]
            </dt>
            <dd class="col-sm-10">
                @Html.DisplayFor(model => model.Guardian.Tel)
            </dd>
            <dt class="col-sm-2">
                @Localizer["email"]
            </dt>
            <dd class="col-sm-10">
                @Html.DisplayFor(model => model.Guardian.Email)
            </dd>
        </dl>
        
        <hr/>
        
        <h5>@Localizer["allergy"]</h5>
        <dl class="row">
            
            @{
                if (Model.Allergies != null)
                {
                    foreach (var allergy in Model.Allergies)
                    {
                        <dt class="col-sm-2">
                            @allergy.Item
                        </dt>
                        <dd class="col-sm-10">
                            @Localizer[allergy.Severity.ToString()]
                        </dd>
                    }
                }
            }
        </dl>
        
        <hr/>

        <h5>@Localizer["medication"]</h5>
        <dl class="row">
            
            @{
                if (Model.Medications != null)
                {
                    foreach (var item in Model.Medications)
                    {
                        <dt class="col-sm-2">
                            @item.Item
                        </dt>
                        <dd class="col-sm-10">
                            @item.Instructions
                        </dd>
                    }
                }
            }
        </dl>
    </div>
    <div>
        <a asp-action="SignUp">@Localizer["anothersignup"]</a>
    </div>

</div>
